#include "main.h"

/**
 * handle_per - Prints a percent symbol
 *
 * @list: unused
 * Return: return the amount of characters printed.
 */
int handle_per(__attribute__((unused))va_list list)
{
	putchar('%');
	return (1);
}
